html
head
  link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css")
body
  ul(class="nav-view") 
    li 
      a(href="/") List 
    li 
      a(href="/todos/create") Add new 
    li
      div(class="")
        form(action="/todos/search", method="GET", class="d-flex")
          input(name="q", type="text", class="form-control d-flex", value=queryString)
          button(class="btn btn-primary") Search
  block content